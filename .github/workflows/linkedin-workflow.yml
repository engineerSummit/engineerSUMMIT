name: Latest LinkedIn Posts
on:
  schedule:
    # Runs every hour
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  update-readme-with-youtube:
    name: Update this repo's README with latest posts from you LinkedIn
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: gautamkrishnar/blog-post-workflow@master
        with:
          comment_tag_name: "LINKEDIN"
          feed_list: "https://www.linkedin.com/in/shivam-gupta-43918219a/detail/recent-activity/shares/"
